# Git HOL-004 – Merge Conflict Resolution

## Objective
Resolve merge conflicts when two branches modify the same file.

## Repository
GitDemo

## Branch Created
GitWork

## File Created
hello.xml

## Commands Covered
- git checkout
- git diff
- git merge
- git status
- git add
- git commit
- git branch -d
- git log

## Result
Successfully created a merge conflict, manually resolved the conflict, committed the merged changes, updated .gitignore, and deleted the merged branch.